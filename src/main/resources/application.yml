spring:
  application:
    name: autenticacao-service
  profiles:
    active: prod
  config:
    import: 'configserver:https://config-server.guilhermejr.net'
  cloud:
    config:
#      discovery:
#        serviceId: config-server
      username: ${CONFIG_USER:config}
      password: ${CONFIG_PASS:l2AeM70weWbpjb1u3R}
#  boot:
#    admin:
#      client:
#        url: ${monitoramentoHost}
#        username: ${monitoramentoUser}
#        password: ${monitoramentoPass}

info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@
    encoding: @project.build.sourceEncoding@
    java:
      version: @java.version@